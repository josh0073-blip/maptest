{
  "name": "farmers-market-vendor-map",
  "version": "1.0.0",
  "private": true,
  "description": "SPA tooling for Farmers Market Vendor Map smoke tests",
  "scripts": {
    "test:smoke": "playwright test",
    "test:smoke:headed": "playwright test --headed",
    "test:smoke:ui": "playwright test --ui",
    "test:smoke:report": "playwright show-report",
    "test:smoke:run-and-report": "playwright test ; playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  },
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
